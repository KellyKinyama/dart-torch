name: dart_torch
description: This library focuses on providing core building blocks for neural networks, including automatic differentiation, and components for Transformer architectures.
version: 1.0.1
repository: https://github.com/KellyKinyama/dart-torch

environment:
  sdk: ^3.6.2

# Add regular dependencies here.
dependencies:
  audio_codec: ^0.1.0
  bishop: ^1.4.4
  chess: ^0.8.1
  fftea: ^1.5.0+1
  image: ^4.8.0
  vector_math: ^2.2.0
  wav: ^1.5.0
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
